# Core: required.
numpy>=1.23
pandas>=1.5
scipy>=1.9
scikit-learn>=1.1        # HistGradientBoostingRegressor with quantile loss

# Data sources: optional, only needed for --ticker.
yfinance>=0.2            # --source yfinance (default)
requests>=2.28           # transport fallback for yfinance behind a proxy
pandas-datareader>=0.10  # --source stooq

# Tests.
pytest>=7.0
